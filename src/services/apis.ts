const BASE_URL_SERVER = "https://dummyjson.com";

// auth endpoints
export const authEndPoints = {
  LOGIN: BASE_URL_SERVER + "/auth/login",
};

// products endpoints
export const postEndPoints = {
  ALL_PRODUCTS: BASE_URL_SERVER + "/products",
  ADD_PRODUCT: BASE_URL_SERVER + "/products/add",
};

// users endpoints
export const usersEndPoints = {
  ALL_USERS: BASE_URL_SERVER + "/users",
};
