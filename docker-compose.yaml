---
version: "3.8"

    
services:
  tech-grow-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tech-grow-frontend
    ports: [4173:4173]

networks:
  proxy:
    external: true